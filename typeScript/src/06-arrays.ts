let prices = [9.99, 14.99, 4.99, 29.99, 19.99];
prices.push(10.99)


//array mixtos
let mixedArray: (number | string | boolean)[] = [12, "hello", true];
mixedArray.push(false);
mixedArray.push("world");
mixedArray.push(42);

let numbers: number[] = [1, 2, 3, 4, 5];

numbers.map(number => number * 2);

let precios: string | number = "hola";
precios = 123;

